name: Build and Public Release

on:
  push:
    tags:
      - 'v*' # Se activa al subir tags como v1.3.5
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          pip install pyinstaller pywebview
          # Agregamos dependencias si tu compat.py o utils.py las necesitan
      
      # --- BUILD WINDOWS (Para Andrés) ---
      - name: Build Windows
        if: matrix.os == 'windows-latest'
        run: |
          pyinstaller --noconfirm --windowed --onefile --name "MATI" \
          --icon="frontend/assets/icon.ico" \
          --add-data "frontend;frontend" \
          --add-data "core;core" \
          main.py
        
      - name: Upload Windows to Release
        if: matrix.os == 'windows-latest'
        uses: softprops/action-gh-release@v2
        with:
          repository: lexrammart/MATI-Releases 
          token: ${{ secrets.RELEASE_TOKEN }}
          target_commitish: main
          tag_name: ${{ github.ref_name }}
          files: dist/MATI.exe

      # --- BUILD MAC (Para tu Mac M1) ---
      - name: Build Mac
        if: matrix.os == 'macos-latest'
        run: |
          # El flag --target-arch universal2 es la clave para que abra en tu M1
          pyinstaller --noconfirm --windowed --name "MATI" \
          --icon="frontend/assets/icon.icns" \
          --target-arch universal2 \
          --add-data "frontend:frontend" \
          --add-data "core:core" \
          main.py

      - name: Zip Mac App
        if: matrix.os == 'macos-latest'
        run: cd dist && zip -r MATI-macOS.zip MATI.app

      - name: Upload Mac to Release
        if: matrix.os == 'macos-latest'
        uses: softprops/action-gh-release@v2
        with:
          repository: lexrammart/MATI-Releases 
          token: ${{ secrets.RELEASE_TOKEN }}
          target_commitish: main
          tag_name: ${{ github.ref_name }}
          files: dist/MATI-macOS.zip
